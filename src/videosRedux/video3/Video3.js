import CounterReducePatron from "./CounterReducePatron";

export default function Video3() {
  // const [count, setCount] = useState(0);
  // const counter = (state) => state + 1;
  //const [state, dispatch] = useReducer(reducer, initialArg, init);
  /* action = {
    type: "INCREMENT",
    amount:1,
  };*/

  return (
    <>
      <CounterReducePatron />
    </>
  );
}
